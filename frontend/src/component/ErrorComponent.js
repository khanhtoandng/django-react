const ErrorComponent = () => (
  <div style={{ display: 'flex', justifyContent: 'center', marginTop: '20px' }}>
    <p style={{ fontSize: '20px', fontWeight: 'bold' }}>Error!</p>
  </div>
);

export default ErrorComponent;
